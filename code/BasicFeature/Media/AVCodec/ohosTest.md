# AVCodecSample 手工测试用例

## 1. 文档说明

本文档用于指导 AVCodecSample 的手工功能、兼容性和稳定性测试，覆盖以下主要链路：

- 文件管理器、图库选取媒体文件；
- 音视频解封装、视频解码、音频解码与播放；
- SurfaceMode 和 BufferMode 视频送显；
- BufferMode 解码帧可选 dump；
- 同步/异步解码、软硬件解码、倍速和画面变换；
- 相机采集、音频采集、音视频编码和 MP4/FLV 封装；
- 权限、异常媒体文件、连续播放录制等异常与稳定性场景。

测试结果建议填写为：

| 结果 | 含义 |
|---|---|
| Pass | 实际结果符合预期 |
| Fail | 实际结果不符合预期，并已记录缺陷 |
| Blocked | 受设备能力、环境或依赖问题阻塞 |
| N/A | 当前设备或版本不适用 |
| 未执行 | 尚未执行该用例 |

## 2. 测试环境

### 2.1 环境信息记录

| 设备型号 | 系统版本 | 测试日期 |
|---|---|---|
| Mate 80 Pro Max | 7.0.0 | 2026.8.3 |
| Mate 60 Pro | 6.1.0 | 2026.8.3 |

### 2.2 前置要求

1. 使用具备相机、麦克风和音频输出能力的真机安装应用。
2. 准备下表所列测试媒体，确保文件本身可以在设备自带播放器中正常播放。
3. 测试录制功能前，确认设备存在足够的媒体存储空间。
4. 测试 dump 前，建议先清理应用沙箱中旧的 `VideoDecoderOut_*` 文件，避免误判。
5. 涉及硬解、H.265、4K、60fps、HDR Vivid 的用例以设备实际能力为准；设备不支持时应验证应用能否提示或回退，结果可记录为 N/A。
6. 除特别说明外，每个播放用例至少观察 30 秒，短视频应完整播放到结束。

### 2.3 建议测试素材

| 素材编号 | 建议规格 | 主要用途 |
|---|---|---|
| M01 | MP4，H.264 + AAC，1920×1080，30fps | 基础播放、软硬解、同步/异步 |
| M02 | MP4，H.265 + AAC，1920×1080，30fps | H.265 硬解、BufferMode |
| M03 | MP4，H.264，仅视频轨 | 纯视频播放 |
| M04 | AAC、MP3、FLAC 或 M4A，仅音频轨 | 纯音频解封装、解码和播放 |
| M05 | MKV，H.264 + AAC | 非 MP4 封装格式 |
| M06 | MPEG-TS，H.264 + AAC | TS 解封装 |
| M07 | 竖屏视频，例如 1080×1920 | 宽高比、旋转和缩放 |
| M08 | 带明显音画同步标记的视频 | 音画同步和倍速 |
| M09 | 大于 5 分钟的视频 | 长时间播放稳定性 |
| M10 | 截断、损坏或不支持的媒体文件 | 异常处理 |

## 3. 基础启动、权限和页面交互

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 结果 |
|---|---|---|---|---|---|
| BASIC-001 | 第一次拉起应用 | 设备正常运行，第一次安装或已清除应用数据 | 启动应用，不执行额外操作。 | 应用正常进入主页面；页面无黑屏、闪退或无响应。 | Pass |
| BASIC-002 | 拒绝相机权限后重新授权 | 第一次安装、已清除应用数据或相机权限已关闭 | 1. 点击 MP4 或 FLV 录制。<br>2. 拒绝相机权限。<br>3. 在随后出现的权限设置弹窗中授予相机权限。 | 应用不崩溃；普通授权被拒绝后拉起权限设置弹窗；授权成功后继续显示录制保存面板。 | Pass |
| BASIC-003 | 拒绝麦克风权限后重新授权 | 第一次安装、已清除应用数据或麦克风权限已关闭 | 1. 点击 MP4 或 FLV 录制。<br>2. 允许相机但拒绝麦克风权限。<br>3. 在随后出现的权限设置弹窗中授予麦克风权限。 | 应用不崩溃；权限设置弹窗只引导处理仍未授予的权限；授权成功后继续显示录制保存面板。 | Pass |
| BASIC-004 | 播放设置弹窗 | 位于主页面，未播放 | 点击播放区域对应的“设置”按钮。 | 弹出四列选择器：解码器类型、SurfaceMode/BufferMode、ASYNC/SYNC、NoDump/Dump。 | Pass |
| BASIC-005 | 播放设置确认 | 已打开播放设置 | 修改任意配置并点击确定。 | 弹窗关闭并显示成功提示；下次播放使用新配置。 | Pass |
| BASIC-006 | 录制设置弹窗 | 位于主页面，未播放 | 点击录制区域对应的“设置”按钮。 | 弹出编码格式、分辨率、帧率和同步模式选择器。 | Pass |
| BASIC-007 | 录制能力回退 | 选择设备不支持的分辨率、帧率或编码格式 | 点击确定。 | 应用不崩溃；提示设备不支持并回退到可用配置，或明确提示失败。 | Pass |
| BASIC-008 | 播放过程中按钮状态 | 已开始播放 | 观察主页面按钮状态。 | 播放按钮显示“播放中”；倍速按钮可用；会影响新任务的设置、播放和录制按钮不可用。 | Pass |
| BASIC-009 | 播放结束按钮恢复 | 正在播放短视频 | 等待播放自然结束。 | 播放按钮恢复；倍速按钮不可用；设置、播放和录制入口恢复可用；应用可再次播放。 | Pass |

## 4. 文件选择和基础播放

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 结果 |
|---|---|---|---|---|---|
| SOURCE-001 | 从文件管理器选择视频 | 已准备 M01 | 1. 点击播放。<br>2. 选择“从文件管理选取”。<br>3. 选择 M01。 | 文件选择器正常打开；选择后自动开始播放；视频画面和音频正常。 | Pass |
| SOURCE-002 | 从图库选择视频 | M01 已导入图库 | 1. 点击播放。<br>2. 选择“从相册选取”。<br>3. 选择 M01。 | 图库选择器正常打开；选择后自动开始播放；视频画面和音频正常。 | Pass |
| SOURCE-003 | 取消文件管理器选择 | 位于主页面 | 打开文件管理器后不选择文件，直接返回。 | 应用返回主页面，无闪退；显示未选择媒体的提示或保持可再次操作状态。 | Pass |
| SOURCE-004 | 取消图库选择 | 位于主页面 | 打开图库后不选择文件，直接返回。 | 应用返回主页面，无闪退；显示未选择视频的提示或保持可再次操作状态。 | Pass |
| SOURCE-005 | 播放纯视频文件 | 已准备 M03 | 使用默认设置播放 M03。 | 视频正常显示；没有音频轨不会导致卡死、闪退或播放无法结束。 | Pass |
| SOURCE-006 | 播放纯音频文件 | 已准备 M04，从文件管理器选取 | 播放 M04。 | 音频正常输出；没有视频轨不会导致闪退；播放完成后按钮状态恢复。 | Pass |
| SOURCE-007 | 播放 MKV | 已准备 M05 | 播放 M05。 | 能正确识别音视频轨；画面和声音正常；播放可结束。 | Pass |
| SOURCE-008 | 播放 MPEG-TS | 已准备 M06 | 播放 M06。 | 能正确解封装和播放；无持续花屏、严重音画不同步或异常退出。 | Pass |
| SOURCE-009 | 竖屏视频显示 | 已准备 M07 | 播放 M07，并切换横竖屏观察。 | 画面保持正确宽高比；无异常拉伸、越界、局部未刷新或持续黑屏。 | Pass |

## 5. 解码模式组合

以下用例优先使用 M01；H.265 用例使用 M02。执行时应同时观察首帧时间、连续性、画面颜色、音频和播放结束状态。

| 用例编号 | 解码器 | 运行模式 |同步模式 | Dump | 预期结果 | 结果 |
|---|---|---|---|---|---|---|
| DECODE-001 | Auto | SurfaceMode | ASYNC | NoDump | 正常起播；视频由解码器 Surface 送显；音视频连续且可正常结束。 | Pass |
| DECODE-002 | Auto | SurfaceMode | SYNC | NoDump | 同步获取输入输出 Buffer 的链路正常；画面、声音和结束状态正常。 | Pass |
| DECODE-003 | Auto | BufferMode | ASYNC | NoDump | 解码器不配置 Surface；应用通过 NativeWindow 拷贝送显；无花屏、色偏或异常裁剪。 | Pass |
| DECODE-004 | Auto | BufferMode | SYNC | NoDump | 同步模式能正确获取宽高、stride、slice height 和像素格式并完成拷贝送显。 | Pass |
| DECODE-005 | Hardware | SurfaceMode | ASYNC | NoDump | 设备支持 M01 硬解时正常播放；不支持时安全失败，不闪退。 | Pass |
| DECODE-006 | Hardware | BufferMode | ASYNC | NoDump | 硬解输出 Buffer 可被应用正确读取并拷贝送显；播放结束后资源正常释放。 | Pass |
| DECODE-007 | Hardware | BufferMode | SYNC | NoDump | H.264/H.265 硬解同步链路正常，无持续花屏或 Buffer 卡死。 | Pass |
| DECODE-008 | Software | SurfaceMode | ASYNC | NoDump | M01 使用软解正常播放；CPU 负载增加不应导致功能异常。 | Pass |
| DECODE-009 | Software | BufferMode | ASYNC | NoDump | 软解输出数据可以正确拷贝送显；颜色、宽高比和播放速度正常。 | Pass |
| DECODE-010 | H.265 硬解 | SurfaceMode | ASYNC | NoDump | 设备支持 H.265 时 M02 正常播放；音视频同步。 | Pass |
| DECODE-011 | H.265 硬解 | BufferMode | ASYNC | NoDump | M02 在 BufferMode 下正常拷贝送显，像素格式处理正确。 | Pass |
| AUDIO-001 | Auto | SurfaceMode | ASYNC | NoDump | 播放 M04；异步回调产生的音频输出 Buffer 被持续消费，声音连续，播放结束后按钮恢复。 | Pass |
| AUDIO-002 | Auto | SurfaceMode | SYNC | NoDump | 播放 M04；同步查询产生的音频输出 Buffer 被持续消费，声音连续，无超时卡死，播放结束后按钮恢复。 | Pass |


## 6. BufferMode dump 与送显

dump 文件目录：

```text
/data/storage/el2/base/haps/entry/files/
```

文件名格式：

```text
VideoDecoderOut_<像素格式>_<宽>_<高>_<时间戳>.yuv
```

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 结果 |
|---|---|---|---|---|---|
| DUMP-001 | BufferMode 默认不 dump | 清理旧 dump 文件；设置 BufferMode、NoDump | 完整播放 M01。 | 视频仍通过拷贝方式正常送显；沙箱目录中不新增 `VideoDecoderOut_*` 文件。 | Pass |
| DUMP-002 | BufferMode 异步 dump | 清理旧文件；设置 BufferMode、ASYNC、Dump | 播放 M01 至结束。 | 播放画面正常；生成一个非空 dump 文件；文件名包含像素格式和实际宽高。 | Pass |
| DUMP-003 | BufferMode 同步 dump | 清理旧文件；设置 BufferMode、SYNC、Dump | 播放 M01 至结束。 | 同步解码和送显正常；生成非空 dump 文件；应用无卡死。 | Pass |
| DUMP-004 | SurfaceMode 忽略 dump | 清理旧文件；设置 SurfaceMode、Dump | 播放 M01 至结束。 | SurfaceMode 正常播放；不会生成 dump 文件，因为实际图像 Buffer 不会交给应用。 | Pass |
| DUMP-005 | dump 内容正确性 | 已完成 DUMP-002 或 DUMP-003 | 1. 导出 dump 文件。<br>2. 按文件名中的像素格式和宽高使用原始图像工具查看。<br>3. 抽查首帧、中间帧和末尾帧。 | 图像内容与播放画面一致；无明显错行、UV 交错错误、颜色异常或 stride 数据写入画面。 | Pass |
| DUMP-006 | dump 文件大小合理 | 已完成一段完整播放并取得帧数 | 检查文件大小。 | YUV420 单帧有效数据约为 `宽×高×3/2`；RGBA 单帧约为 `宽×高×4`；总大小与实际写入帧数基本一致。 | Pass |
| DUMP-007 | dump 与送显同时开启 | BufferMode、Dump | 连续播放 M09 至少 5 分钟，观察画面和文件增长。 | dump 过程中仍持续拷贝送显；无长期黑屏、死锁、Buffer 耗尽或应用退出。 | Pass |
| DUMP-008 | 从 Dump 切换到 NoDump | 先完成一次 Dump 播放 | 1. 记录现有文件。<br>2. 设置 NoDump。<br>3. 再次播放 M01。 | 第二次播放正常；不会新建 dump 文件，也不会继续写入上一轮文件。 | Pass |

## 7. 画面变换、倍速和音画同步

### 7.1 画面变换

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| TRANSFORM-001 | 旋转 0/90/180/270 度 | 播放 M07，依次选择四种旋转。 | 每次选择后画面方向符合设置；窗口无持续黑屏；宽高变化时显示区域正确。 | Pass |
| TRANSFORM-002 | 水平/垂直翻转 | 播放带方向标记的视频，分别选择水平翻转和垂直翻转。 | 左右或上下关系符合预期；无撕裂、花屏和旧画面残留。 | Pass |
| TRANSFORM-003 | 翻转与旋转组合 | 依次选择水平/垂直翻转并旋转 90、180、270 度的组合项。 | 组合变换结果正确；连续切换不会崩溃或导致后续帧停止显示。 | Pass |
| TRANSFORM-004 | BufferMode 变换 | 设置 BufferMode 后重复 TRANSFORM-001 和 TRANSFORM-002。 | transform 能传递到用于拷贝送显的 NativeWindow；效果与 SurfaceMode 一致。 | Pass |

### 7.2 倍速和音画同步

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 结果 |
|---|---|---|---|---|---|
| SPEED-001 | X1/X2/X3 倍速切换 | 播放 M08 | 依次选择 X1、X2、X3，每种速度观察至少 10 秒。 | 播放速度符合选择；无死锁、持续静音或画面停止；提示信息正确。 | Pass |
| SPEED-002 | 长按临时两倍速 | 正在以 X1 播放 | 长按播放窗口至少 5 秒。 | 长按期间切换为 X2；应用保持响应。 | Pass |
| SPEED-003 | 松手恢复原速 | 正在长按两倍速 | 松开手指。 | 提示恢复 X1；后续播放速度恢复正常。 | Pass |
| SPEED-004 | 连续切换倍速 | 正在播放 M08 | 在 X1/X2/X3 之间连续切换 20 次。 | 无崩溃、卡死、音频队列异常增长或视频永久停止。 | Pass |
| SYNC-001 | 一倍速音画同步 | 播放 M08 | 观察口型、敲击或时间标记至少 1 分钟。 | 音画同步稳定，无持续可感知偏移。 | Pass |
| SYNC-002 | 倍速音画同步 | 播放 M08 并切换 X2/X3 | 分别观察至少 30 秒。 | 视频丢帧/等待逻辑工作正常；无持续累积的音画偏移。 | Pass |
| SYNC-003 | Surface/Buffer 同源一致性 | 使用相同设置分别以 SurfaceMode 和 BufferMode 播放 M08 | 对比起播、时长、倍速和结束状态。 | 两种模式的播放进度和倍速行为基本一致；BufferMode 不因拷贝送显改变媒体时间线。 | Pass |

## 8. 录制、编码和封装

| 用例编号 | 测试项 | 前置条件 | 操作步骤 | 预期结果 | 结果 |
|---|---|---|---|---|---|
| RECORD-001 | 进入录制页面 | 已允许相机、麦克风权限 | 1. 点击“录制 MP4”。<br>2. 在保存确认页点击保存并允许系统授权。 | 成功跳转到录制页面；相机预览正常；无黑屏或闪退。 | Pass |
| RECORD-002 | H.264 MP4 异步录制 | 设置 H264、1080P、30fps、ASYNC | 录制 10 秒后停止。 | 返回主页面；图库生成 MP4；文件含正常视频和音频，可完整播放。 | Pass |
| RECORD-003 | H.264 MP4 同步录制 | 设置 H264、1080P、30fps、SYNC | 录制 10 秒后停止。 | 同步编码链路正常；生成可播放 MP4；时间戳连续。 | Pass |
| RECORD-004 | H.265 MP4 录制 | 设备支持 H.265 编码 | 设置 H265、1080P、30fps，录制 10 秒。 | 生成 H.265 视频轨和音频轨；系统或支持 H.265 的播放器可正常播放。 | Pass |
| RECORD-005 | FLV 录制 | 选择 H264、1080P、30fps | 点击“录制 FLV”，录制 10 秒后停止。 | 生成 `.flv` 文件；封装可被支持 FLV 的分析工具识别；音视频可正常解码。 | Pass |
| RECORD-006 | 720P/1080P 分辨率 | 设备支持对应 profile | 分别录制 720P 和 1080P 文件。 | 输出文件分辨率与设置一致，或设备不支持时按提示回退；画面比例正常。 | Pass |
| RECORD-007 | 4K/60fps 能力处理 | 设备可能不支持对应 profile | 分别选择 4K、60fps 并确认。 | 支持时按所选规格录制；不支持时提示并回退；应用不崩溃。 | Pass |
| RECORD-008 | HDR Vivid 录制 | 设备支持相应编码与相机能力 | 选择 HDRVivid 后录制。 | 支持设备生成正确的 HDR 视频；不支持设备有明确提示或回退，不闪退。 | Pass |
| RECORD-009 | 开始按钮防重复点击 | 位于录制页面 | 快速连续点击开始录制。 | 不会重复创建录制任务；按钮在保护时间内不可重复触发；计时器只启动一次。 | Pass |
| RECORD-010 | 停止并保存 | 已录制至少 3 秒 | 点击停止录制并等待返回主页面。 | 编码器收到 EOS；封装器正常结束；文件尾完整；图库文件可播放。 | Pass |
| RECORD-011 | 相机双指缩放 | 位于录制预览页 | 多次双指放大、缩小。 | 预览画面随 zoom ratio 变化；不超过设备允许范围；无崩溃。 | Pass |
| RECORD-012 | 录制文件轨道检查 | 已完成任一录制 | 使用媒体信息工具检查输出文件。 | 存在预期的视频轨和 AAC 音频轨；编码格式、分辨率、帧率、时长与设置基本一致。 | Pass |
| RECORD-013 | 连续录制 | 存储空间充足 | 连续完成 10 次“进入录制—开始—停止”。 | 每次都生成独立且可播放的文件；相机、麦克风和编码资源可重复创建释放。 | Pass |

## 9. 异常处理与稳定性

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 结果 |
|---|---|---|---|---|
| EXCEPTION-001 | 损坏或不支持的媒体文件 | 尝试播放 M10。 | 显示“媒体文件已损坏或格式不受支持，无法播放”的提示；应用不闪退、不长期无响应；Native 资源释放完成后按钮恢复操作。 | Pass |
| EXCEPTION-002 | 空文件 | 创建并选择大小为 0 的媒体文件。 | 显示“媒体文件为空，无法播放”的提示；不启动 Native 播放任务；应用不崩溃且按钮保持可操作。 | Pass |
| EXCEPTION-003 | 播放中切换横竖屏 | 播放 M01 时多次旋转设备。 | XComponent 尺寸更新正常；画面可继续显示；无资源泄漏或异常退出。 | Pass |
| EXCEPTION-004 | 播放中切换前后台 | 播放 M01 时进入后台，再返回前台。 | 应用不崩溃；返回后页面状态明确，能够继续或重新开始播放。 | Pass |
| EXCEPTION-005 | 录制中切换前后台 | 录制过程中进入后台，再返回前台。 | 应用不崩溃；相机和录制状态明确；停止后文件不应导致系统媒体库异常。 | Pass |
| STABILITY-001 | 长时间 SurfaceMode 播放 | SurfaceMode 播放 M09 至少 5 分钟。 | 无崩溃、死锁、持续卡顿、严重音画漂移或内存持续异常增长。 | Pass |
| STABILITY-002 | 长时间 BufferMode 播放 | BufferMode、NoDump 播放 M09 至少 5 分钟。 | NativeWindow Buffer 持续申请、Flush、归还正常；无 Buffer 耗尽和长期黑屏。 | Pass |
| STABILITY-003 | 长时间 BufferMode dump | BufferMode、Dump 播放 M09 至少 5 分钟。 | 显示和文件写入同时进行；文件持续增长；播放完成后文件关闭且资源释放。 | Pass |
| STABILITY-004 | 连续播放 | 连续播放 M01 20 次，交替使用 SurfaceMode/BufferMode 和 SYNC/ASYNC。 | 每轮都能正常开始和结束；线程、解码器、音频渲染器和窗口资源可重复创建释放。 | Pass |
| STABILITY-005 | 播放录制交替 | 依次执行“播放—录制—播放—录制”至少 10 轮。 | 播放和录制资源互不残留；相机、麦克风、解码器和编码器均可再次使用。 | Pass |

### 9.1 本轮释放生命周期优化回归


| 用例编号 | 测试项目 | 前置条件与操作 | 预期结果 | 结果 |
|---|---|---|---|---|
| LIFECYCLE-001 | 异步音视频自然结束后再次播放 | 使用 M01，选择 ASYNC；等待播放自然结束，再次选择同一文件播放。 | 首轮和第二轮均正常结束；无死锁、崩溃、黑屏或按钮无法恢复。 | Pass |
| LIFECYCLE-002 | 同步音视频自然结束后再次播放 | 使用 M01，选择 SYNC；等待播放自然结束，再次选择同一文件播放。 | 首轮和第二轮均正常结束；输入/输出线程均能退出，资源可再次创建。 | Pass |
| LIFECYCLE-003 | 纯音频与纯视频释放 | 分别播放 M03 和 M04，等待自然结束后再次播放另一种媒体。 | 缺少单一轨道时释放协调不等待不存在的线程；两种媒体均可正常播放和结束。 | Pass |
| LIFECYCLE-004 | 播放错误后的恢复 | 选择损坏或空文件触发播放失败，再选择 M01 播放。 | 错误任务安全结束；不会长期占用解码器、渲染器或回调上下文；M01 可正常播放。 | Pass |
| LIFECYCLE-005 | 连续交替播放释放压力 | 交替使用 SurfaceMode/BufferMode、SYNC/ASYNC 播放 M01 共 20 轮。 | 每轮均可开始和结束；无线程泄漏、卡死、崩溃、Buffer 耗尽或后续无法播放。 | Pass |

## 10. 测试执行汇总

| 分类 | 用例总数 | Pass | Fail | Blocked | N/A | 未执行 |
|---|---:|---:|---:|---:|---:|---:|
| 基础启动、权限和页面交互 | 9 | 9 | 0 | 0 | 0 | 0 |
| 文件选择和基础播放 | 9 | 9 | 0 | 0 | 0 | 0 |
| 解码模式组合 | 13 | 13 | 0 | 0 | 0 | 0 |
| BufferMode dump 与送显 | 8 | 8 | 0 | 0 | 0 | 0 |
| 画面变换、倍速和音画同步 | 11 | 11 | 0 | 0 | 0 | 0 |
| 录制、编码和封装 | 13 | 13 | 0 | 0 | 0 | 0 |
| 异常处理与稳定性 | 15 | 15 | 0 | 0 | 0 | 0 |
| 合计 | 78 | 78 | 0 | 0 | 0 | 0 |
