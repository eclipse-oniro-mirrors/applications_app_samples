import { XMLNode } from '../xml-utils.js';
/**
 * AZ互通中对xml进行转换操作的抽象类
 *
 * @since 2022/6/1
 */
export declare abstract class XmlTransformer {
    private _log;
    protected readonly xmlNode: XMLNode;
    private readonly dest;
    constructor(src: string, dest: string);
    /**
     * 执行要对这个XML节点执行的操作
     */
    protected abstract transformNode(): void;
    /**
     * 执行transform动作
     * 1. 在constructor中已经确认好输入和输出
     * 2. 替换namespace，确认所有的xml都要做这个步骤
     * 3. transformNode方法执行要对这个节点执行的操作，继承类只需要实现该方法
     * 4. 将结果输出到输出中
     */
    transform(): void;
    /**
     * 替换namespace
     * @private
     */
    private addNamespace;
    /**
     * 将结果输出到输出中
     * @private
     */
    private outputXMLToFile;
}
