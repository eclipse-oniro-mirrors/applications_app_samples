import { OhosHapTask } from '../task/ohos-hap-task.js';
import { TargetTaskService } from '../service/target-task-service.js';
/**
 * pack dex and res into shell
 */
export declare class PackShell extends OhosHapTask {
    private static readonly BEGIN_TIME;
    private static readonly STORE;
    constructor(taskService: TargetTaskService);
    protected initTaskDepends(): void;
    protected taskShouldDo(): boolean;
    protected beforeTask(): void;
    protected doTaskAction(): void;
    protected packShell(): void;
    private packCutSHell;
}
