/**
 * 读取项目目录下local.properties文件
 *
 * @since 2021-12-16
 */
export declare class Property {
    private _log;
    private static instance;
    private _properties;
    constructor();
    static getInstance(): Property;
    /**
     * 读取当前命令行执行的路径下的local.properties的内容
     *
     * @returns {string} local.properties的内容
     */
    private static _readFile;
    /**
     * 读取路径时将分隔符转为正斜杆
     * eg: a\\b\\c -> a/b/c
     *
     * @param {string} oriString 原始字符串
     * @returns {string} 转化后的字符串
     */
    static normalizePathSeparator(oriString: string): string;
    getProperty(key: string): string;
    getNodeJsDir(): string;
    getSdkDir(): string;
    getHosSdkDir(): string;
    private validatePath;
    private _readPath;
    private _setPathIfNotExist;
    private _setProperty;
}
