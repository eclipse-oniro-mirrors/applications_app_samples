import { ProjectModel } from '../../model/project/project-model.js';
import { ModuleModel } from '../../model/module/module-model.js';
import { ModuleDependencyInfo } from './module-dependency-info.js';
/**
 * 管理har的依赖，提供获取当前模块依赖的数据信息
 *
 * @since 2022/5/7
 */
export declare class DependencyManager {
    private _log;
    private _allDependency;
    private _model;
    private readonly _projectModel;
    private readonly _moduleName;
    private readonly _isFaMode;
    constructor(isFaMode: boolean, model: ModuleModel | ProjectModel, projectModel?: ProjectModel);
    createModelDependencyInfo(): ModuleDependencyInfo;
    private collectHarDependency;
    /**
     * 通过config.json/module.json5判断当前模块是否为har模块
     *
     * @param {string} pkgPath 当前模块的路径
     * @returns {boolean}
     * @private
     */
    private isValidHarByProfile;
    /**
     * 检查当前模块的moduleType是否标记为har，且model与hap是否一致
     *
     * @param {string} runtimeJson config.json/module.json5
     * @param {boolean} harIsFAMode har包是否为FA model
     * @returns {boolean}
     * @private
     */
    private checkHarStatus;
    /**
     * 1. 根据config.json/module.json5判断是不是har
     * 2. package.json中需要有ohos闭包
     * 3. 考虑循环依赖，用set过滤重复项，向queue中添加package.json路径
     *
     * @param {string} pkgPath 当前模块的路径
     * @param {OhosPackageJson} devPkgJsonPathObj 当前模块的package.json对象
     * @returns {boolean}
     * @private
     */
    private isNotHar;
}
