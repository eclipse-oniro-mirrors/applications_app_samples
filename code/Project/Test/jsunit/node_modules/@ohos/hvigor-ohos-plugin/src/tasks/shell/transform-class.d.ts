import { TargetTaskService } from '../service/target-task-service.js';
import { OhosShellHapTask } from './ohos-shell-hap-task.js';
/**
 * transform class
 */
export declare class TransformClass extends OhosShellHapTask {
    private _log;
    constructor(taskService: TargetTaskService);
    protected initTaskDepends(): void;
    protected taskShouldDo(): boolean;
    protected beforeTask(): void;
    protected doTaskAction(): void;
    private transformClass;
    /**
     * 遍历classDir目录, 并在指定位置生成所有字节码文件
     *
     * @param classDir 起始遍历的目录
     * @param classFilePath 生成的文件路径
     */
    private generateClassesFiles;
    /**
     * 按照正则规则遍历目录, 保存符合规则的文件路径到list中
     *
     * @param dir 开始遍历的目录
     * @param list 保存的list
     * @param regex 正则规则
     */
    listFile(dir: string, list: string[], regex?: RegExp | undefined): string[];
}
