import { ProjectModel } from '../../model/project/project-model.js';
export interface ExtraInfo {
    /**
     * 工程模型状态
     *
     * @return true:对应module.json5,false:对应config.json
     */
    isStageMode(): boolean;
}
/**
 * App级别项目的标识信息
 *
 * @since 2022/1/18
 */
export declare class AppExtraInfo implements ExtraInfo {
    private readonly _isSupportHos;
    constructor(projectModel: ProjectModel);
    isStageMode(): boolean;
}
